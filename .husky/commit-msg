#!/usr/bin/env sh
# Hikari Protocol — commit-msg hook.
# Enforces Conventional Commits with Hikari Protocol scopes via commitlint.
# The config lives in commitlint.config.mjs.

npx --no-install commitlint --edit "$1"
